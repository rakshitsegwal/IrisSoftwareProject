public class datechecker {

    /*
     


List<Opportunity> opportunitiesThisYear = [
    SELECT Id, Name, CloseDate
    FROM Opportunity
    WHERE CALENDAR_YEAR(CloseDate) = :System.today().year()
];

System.debug(opportunitiesThisYear);

*/
}